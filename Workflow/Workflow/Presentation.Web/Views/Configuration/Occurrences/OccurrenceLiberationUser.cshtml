@model Presentation.Web.Models.Configuration.OccurrencesViewModel
@if (Model.LiberationUsers != null) {
    <div class="row">
        @for (int i = 0; i < Model.LiberationUsers.Count; i++) {
            <div class="col-md-6 col-lg-4">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-sm-1 col-md-2 col-lg-1">
                                <input type="hidden" name="@Html.NameFor(x => Model.LiberationUsers[i].ParentId)" value="@(Model.LiberationUsers[i].ParentId)" />
                                <input type="hidden" name="@Html.NameFor(x => Model.LiberationUsers[i].UserId)" value="@(Model.LiberationUsers[i].UserId)" />
                                <input type="checkbox" value="true" class="EditControl"
                                       name="@Html.NameFor(x => Model.LiberationUsers[i].IsChecked)"
                                       @(Model.LiberationUsers[i].IsChecked ? "checked" : "") 
                                       @(Model.IsEditable ? "": "disabled")/>
                            </div>
                            <div class="col-sm-11 col-md-10 col-lg-11">
                                <label>@(Model.LiberationUsers[i].Name)</label>
                            </div>
                            <div class="col-sm-1 col-md-2 col-lg-1">
                                &nbsp;
                            </div>
                            <div class="col-sm-11 col-md-10 col-lg-11">
                                <label>@(Model.LiberationUsers[i].Email)</label>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}